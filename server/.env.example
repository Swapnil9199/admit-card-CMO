# Optional Custom SMTP Settings (e.g. Gmail / SendGrid / Amazon SES / Brevo)
# If left empty, the server automatically uses Ethereal test mailer for seamless local testing.
PORT=5000
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Combine Mentor Official" <no-reply@combinementor.in>
